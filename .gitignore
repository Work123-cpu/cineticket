# Environment variables (contains secret tokens — NEVER commit this!)
.env
.env.local

# Dependency directories
node_modules/

# User task data (dynamic, changes at runtime — stored on Railway Volume)
data/checks.json

# Audit logs (generated at runtime)
logs/
*.log

# OS & Editor files
.DS_Store
Thumbs.db
.vscode/
.idea/

# config/cinemas.json is INTENTIONALLY committed to Git (static theater config)
# Do NOT add config/ to this file!
